# Планы проекта

> Этот файл ведёт агент-планировщик.
> Новый или обновлённый план записывается только после явного одобрения пользователя.
> Исполнитель работает только с планом, у которого статус `approved` или `in_progress`.

## Активный план

### PLAN-20260901-09 — Заглушка аватара на главном экране

**Статус:** `approved`  
**Утверждён:** 2026-09-04  
**Ветка:** `ai/feature-avatar-placeholder`

**Задача:** Если у пункта расписания на главном экране отсутствует любая из аватарок (`card_avatar_url` или `avatar_url`) — показывать заглушку с первой буквой названия площадки, чтобы не ломалась сетка элементов.

**Решение (подтверждено пользователем):**
- В `src/components/PlatformCard.tsx` при отсутствии `card_avatar_url` и `avatar_url` рендерить заглушку `<div className="kaf-avatar kaf-avatar-placeholder">` с первой буквой `platform.name` (круглая, 48px).
- `src/components/Avatar.tsx` и `src/components/PlatformDetail.tsx` НЕ менять (заглушка только на главном экране).
- Дополнить `.kaf-avatar-placeholder` в `src/styles/globals.css`: `border-radius: 50%`, `flex-shrink: 0` (круглая, не ломает сетку).
- Обновить/добавить тесты.

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-49).

**Критерии готовности плана:**
- [x] Архитектура согласована с пользователем.
- [x] Задачи декомпозированы, критерии готовности и проверки заданы.
- [x] Учтён `.agents/changelog.md`.

---

### PLAN-20260901-08 — Исправление кнопки «назад» (история навигации)

**Статус:** `approved`  
**Утверждён:** 2026-09-04  
**Ветка:** `ai/fix-back-navigation`

**Задача:** Исправить кнопку «назад», которая всё ещё возвращает сразу на главный экран вместо предыдущего экрана приложения.

**Причина (диагностика планировщика):**
- В `src/lib/router.ts` функция `navigate()` пушит текущий маршрут в историю только если `!isAuthHash(current) && !isAuthHash(hash)`.
- При переходе на ask/review (`hash='ask/p1'`) условие `!isAuthHash(hash)` ложно → площадка (`current='p1'`) НЕ сохраняется в стек.
- Следствие: сценарий «главная → площадка → ask → «назад»» возвращает на главную, а не на площадку (стек содержит только `''`).
- TASK-47 был ошибочно закрыт executor как «уже реализовано в TASK-39» — реальная проблема не исправлена.

**Решение (подтверждено пользователем):**
- В `navigate()` пушить `current` всегда, если `current` не auth (независимо от того, является ли `hash` auth). Это сохраняет площадку при переходе на ask/review.
- При submit (возврат на площадку с ask/review), если верхушка стека совпадает с целевым маршрутом — убрать её (`popNavigation()`), чтобы избежать двойного «назад» после submit.
- `goBack()` и синхронизация с `hashchange` браузера остаются без изменений (проверить, что не ломаются).
- Добавить тесты для сценариев: ask → «назад» → площадка; review → «назад» → площадка; submit → «назад» → главная (один раз).

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-48).

**Критерии готовности плана:**
- [x] Архитектура согласована с пользователем.
- [x] Задачи декомпозированы, критерии готовности и проверки заданы.
- [x] Учтён `.agents/changelog.md`.

---

### PLAN-20260901-07 — Анонимные пользователи, авторизация, дата, подтверждение удаления, навигация

**Статус:** `completed`  
**Утверждён:** 2026-09-04  
**Завершён:** 2026-09-04  
**Ветка:** `ai/fix-anon-auth-date`

**Задача:** Исправить новую порцию задач/проблем:
1. Для анонимных пользователей не отображаются уже заданные вопросы — анонимный id должен быть стабильным на время сессии (sessionStorage), чтобы вопросы привязывались к анониму.
2. Авторизация показывается каждый раз в VK — должна показываться только один раз за сессию и запоминаться на время сессии.
3. На главном экране должно отображаться не только время, но и дата мероприятия в формате «5 сентября, 14:00-16:00».
4. Плашка подтверждения удаления — не через `alert`/`window.confirm`, а самодельная, в стилистике приложения.
5. Хранить логин через ВК на время сессии (до закрытия вкладки).
6. Кнопка «назад» возвращает сразу на главный экран — история навигации не работает корректно.

**Решения (подтверждены пользователем):**
- Анонимный id — перевести `getFallbackIdentity`/`setFallbackIdentity` на sessionStorage; в `AskScreen`/`ReviewScreen` вне VK использовать сгенерированный уникальный id (не жёстко закодированный `'anon'`), стабильный на время сессии.
- Авторизация — в `AskScreen`/`ReviewScreen` при инициализации читать `getStoredProfile()` из sessionStorage; если профиль есть (авторизовался ранее в сессии) — не показывать auth.
- Дата — в `PlatformCard` добавить дату в формате «5 сентября, 14:00-16:00».
- Подтверждение удаления — создать кастомный модальный компонент в стилистике приложения; заменить `window.confirm` в `PlatformDetail` и `QuestionForm`.
- Логин на время сессии — `setStoredProfile` уже в sessionStorage; перевести fallback id на sessionStorage.
- Навигация — исправить историю навигации, чтобы «назад» возвращал на предыдущий экран.

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-43 … TASK-20260901-47).

**Критерии готовности плана:**
- [x] Архитектура согласована с пользователем.
- [x] Задачи декомпозированы, критерии готовности и проверки заданы.
- [x] Учтён `.agents/changelog.md`.

---

### PLAN-20260901-06 — Исправление проблем после переразвёртывания GAS

**Статус:** `approved`  
**Утверждён:** 2026-09-03  
**Ветка:** `ai/fix-test3`

**Задача:** Исправить новую порцию проблем, выявленных пользователем после переразвёртывания GAS:
1. `404 (Not Found)` на `script.googleusercontent.com/macros/echo` после отправки вопроса — `fetchPlatform` (GET) возвращает 404. URL в `.env.local` обновлён, но 404 остаётся.
2. Кнопка «назад» возвращает сразу на главный экран (история навигации не работает корректно).
3. Очень долгая отправка ответов и отзывов (GAS cold start / долгие POST-запросы).
4. При первом открытии площадки не отображаются уже заданные вопросы.

**Решения (подтверждены пользователем):**
- 404 GAS — URL обновлён, но 404 остаётся. Нужно улучшить обработку ошибок в `api.ts` (понятное сообщение, не блокировать UI) и задокументировать проверку настроек GAS (доступ «Anyone», `/exec` URL).
- История навигации — исправить, чтобы «назад» возвращал на предыдущий экран (площадка → главная, ask → площадка и т.д.), а не сразу на главную.
- Долгая отправка — показывать индикатор загрузки при отправке вопроса/отзыва, не блокировать UI.
- Вопросы при первом открытии площадки — исправить загрузку вопросов (без кэша).

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-39 … TASK-20260901-42).

**Критерии готовности плана:**
- [x] Архитектура согласована с пользователем.
- [x] Задачи декомпозированы, критерии готовности и проверки заданы.
- [x] Учтён `.agents/changelog.md`.

---

### PLAN-20260901-05 — Исправление регрессий и новых проблем после TASK-26…33

**Статус:** `approved`  
**Утверждён:** 2026-09-03  
**Ветка:** `ai/fix-test3`

**Задача:** Исправить новую порцию ошибок, выявленных пользователем после работы executor по TASK-26…33:
1. Долгая загрузка страницы площадки (GAS cold start 20–50с + 404).
2. «Назад» возвращает на главный экран, игнорируя историю (браузер вне VK), нестабильно.
3. Долгий (10с) запрос `script.googleusercontent.com/macros/echo` (GAS redirect).
4. Центрирование «Имя», «Отзыв», «Вопрос» — сделать по левому краю, шрифт как на кнопках (17px). Остальное без изменений.
5. Запросы очень долгие (20–50с).
6. `404 (Not Found)` на `script.googleusercontent.com/macros/echo` (GAS web app не развёрнут — пользователь переразвернёт сам, вне кода).
7. Не отображаются уже заданные вопросы на странице площадки.
8. Hydration error при открытии вне VK по прямой ссылке на площадку (`Expected server HTML to contain a matching <button> in <header>`); после ошибки не отображается кнопка «назад».

**Решения (подтверждены пользователем):**
- 404 GAS — вне кода, пользователь переразвернёт GAS web app сам.
- История навигации — исправить, чтобы «назад» возвращал на предыдущий маршрут, а не на главную.
- Центрирование — НЕ отменять полностью: «Имя», «Отзыв», «Вопрос» выровнять по левому краю (шрифт как на кнопках, 17px), остальное (звёзды) без изменений.
- Hydration error — отключить SSR для `AppRouter` (рендерить только на клиенте), т.к. сервер всегда рендерит schedule, а клиент по хэшу — другой экран.
- Долгая загрузка площадки — улучшить кэш: показывать кэш сразу, обновлять в фоне.
- Вопросы на странице площадки — проверить после переразвёртывания GAS; исправить кэш, чтобы не показывал устаревшие данные без вопросов.

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-34 … TASK-20260901-38).

**Критерии готовности плана:**
- [x] Архитектура согласована с пользователем.
- [x] Задачи декомпозированы, критерии готовности и проверки заданы.
- [x] Учтён `.agents/changelog.md`.

---

### PLAN-20260901-04 — Доработки после ручного тестирования: UI, авторизация, кеширование

**Статус:** `approved`  
**Утверждён:** 2026-09-03  
**Ветка:** `ai/fix-test3`

**Задача:** Исправить 11 проблем/дополнений, выявленных при ручном тестировании:
1. Стрелка назад + её обводка не центрированы; иконка текстовая → должна быть графикой под стиль ВК.
2. «Имя», «Отзыв» и звёзды не центрированы на экранах отзыва и вопроса.
3. GAS: 30с на холодную, 10с на горячую — оптимизация.
4. Подтверждение для кнопки «Удалить».
5. Хранить логин через ВК на время сессии (до закрытия вкладки).
6. Если приложение запущено не через ВК — не показывать экран авторизации.
7. На странице «задать вопрос» не отображать уже заданные вопросы; убрать обращение к GAS и задержку.
8. Добавить кеширование расписания и содержимого площадок.
9. Кнопка «назад» не должна возвращать на экран авторизации.
10. Пункт расписания, время которого прошло, отображается не серым (ф-я «сегодня» работает).
11. WebSocket HMR ошибка (dev-only, задокументировать).

**Решения (подтверждены пользователем):**
- Стрелка: SVG-иконка из `@vkontakte/icons` (например `Icon28ChevronLeftOutline`) + точное центрирование (flex, padding 0).
- Центрирование: обернуть форму Ask в `.kaf-center` (как Review); центрировать поля и звёзды.
- Задержка отправки: 10с → 0.5с.
- Сессионный логин: `setStoredProfile`/`getStoredProfile` → sessionStorage.
- Авторизация: в VK — показывать auth (явное согласие); вне VK — не показывать, использовать fallback-профиль. Определение VK-окружения — `isVkEnvironment()` по launch params в URL.
- Ask-экран: убрать `fetchPlatform` и блок «Мои вопросы».
- «Назад»: ask/review маршруты НЕ пишутся в историю навигации (страница авторизации не возвращается).
- Серый цвет: `isPast()` (time_start/end завершились) → `.kaf-card.is-past`.
- Кеширование: добавить кэш содержимого площадок (`fetchPlatform`) в localStorage по id + версия кеша.
- Подтверждение удаления: `window.confirm`.
- GAS cold start — свойство платформы, смягчается кешированием; WebSocket HMR — dev-only, документируется.

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-26 … TASK-20260901-33).

**Критерии готовности плана:**
- [x] Архитектура согласована с пользователем.
- [x] Задачи декомпозированы, критерии готовности и проверки заданы.
- [x] Учтён `.agents/changelog.md`.

---

## История

### PLAN-20260901-03 — Исправления после ручного тестирования: навигация, авторизация, оптимизация (выполнен)

**Статус:** `completed`  
**Утверждён:** 2026-09-03  
**Завершён:** 2026-09-03  
**Ветка:** `ai/test-plan2`  
**Коммит:** `c9c71f9`

**Задача:** Исправить 8+ проблем, выявленных при ручном тестировании:
1. История навигации приложения (sessionStorage) + кнопка «назад».
2. Кнопки «редактировать»/«удалить» на странице площадки → унифицированная страница редактирования (AskScreen).
3. Таймаут VK bridge (3–5 сек) для работы без VK.
4. Авторизация: всегда показывать auth screen перед отправкой (явное согласие).
5. Карточка без аватара — блок перестраивается.
6. Стрелка назад — центрирование.
7. Шрифты, центрирование, проверка Content-Type.
8. Анализ и оптимизация скорости (сетевые + внутренние).

**Решения:**
- История навигации: стек в `sessionStorage`, `navigate()` вызывает `pushNavigation()`.
- Кнопка «назад»: `goBack()` извлекает последний маршрут из стека; fallback на расписание.
- Кнопки ред./удал.: на странице площадки → AskScreen в режиме editing.
- Таймаут VK bridge: `Promise.race` с 3–5 сек; fallback на localStorage / анонимный профиль.
- Авторизация: auth-gate всегда (даже если профиль сохранён).
- Avatar: `null` без URL; карточка перестраивается через flex.
- Стрелка: `line-height: 1` для точного центрирования.

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-18 … TASK-20260901-25).

---

### PLAN-20260901-02 — UI-баги: тулбар главного экрана, аватарки, экран отзыва (выполнен)

**Статус:** `completed`  
**Утверждён:** 2026-09-01  
**Ветка:** `ai/feature-kaf-feedback`

**Задача:** Исправить 6 UI-багов в трёх экранах:
1. Главный экран — закреплённый полупрозрачный тулбар (liquid glass) с «КАФ» по центру большим шрифтом и «только сегодня» (без кнопки «Войти»).
2. Главный экран — увеличить мелкие и нечитаемые надписи.
3. Главный экран — аватарка карточки из отдельной колонки `card_avatar_url` (не из `avatar_url`).
4. Экран площадки — аватарка рядом с именем докладчика (`speaker`), а не с названием; использовать `avatar_url`.
5. Экран площадки — описание выделить в белую рамку со скруглениями (`kaf-glass`).
6. Экран отзыва — центрировать «Имя», «Отзыв» и звёзды; увеличить размер звёзд.

**Решения:**
- Тулбар — только на главном экране (остальные экраны сохраняют `AppShell`-заголовок). Кнопки «Войти» в тулбаре нет.
- Центрирование полей и звёзд — только на экране отзыва.
- Новая колонка `card_avatar_url` в листе «площадки»; при отсутствии — placeholder.
- CORS на POST: отправлять `Content-Type: text/plain` (простой запрос, без preflight), GAS-код не меняется.
- Авторизация: появляется только при открытии «задать вопрос»/«оставить отзыв» (auth-gate); имя подтягивается из VK и подставляется в форму.
- TASK-01, 03, 04, 14 затрагивают общие файлы — выполняются последовательно; TASK-02, 13 (без общих файлов) — параллельно.
- Тесты: `lib/router.ts`, `lib/storage.ts`, `lib/identity.ts` (jsdom); `lib/api.ts` (fetch mock); компоненты `StatusView`, `StarRating`, `Avatar`, `QuestionForm`, `ReviewForm` (testing-library).

**Декомпозиция задач:** TASK-20260901-09 … TASK-20260901-17.

---

### PLAN-20260901-01 — VK Mini App «КАФ'26» (выполнен)

**Статус:** `completed`  
**Утверждён:** 2026-09-01  
**Ветка:** `ai/feature-kaf-feedback`

**Задача:** Разработать VK Mini App для Красноярского астрономического форума «КАФ'26»: расписание площадок, вопросы и отзывы, авторизация через VK ID.

**Стек:**
- Фронтенд: Next.js (TypeScript), статический экспорт, многоэкранный SPA с хэш-роутингом, мобильная адаптация.
- UI: `@vkontakte/vkui` (тема liquid glass) + `@vkontakte/icons` + кастомный CSS. Markdown: `react-markdown`.
- Бэкенд: Google Apps Script (веб-апп), данные в Google Таблице (листы «площадки», «вопросы», «отзывы», служебная «версия кеша»).
- Публикация: GitHub Pages (корень домена, без basePath).

**Решения (подтверждены пользователем):**
1. Время в таблице — ISO 8601 UTC без смещения. GAS нормализует в UTC epoch ms.
2. UI — VKUI + кастомный CSS liquid glass.
3. GitHub Pages — корень домена (без basePath).
4. ID VK Mini App — плейсхолдер/переменная окружения (`NEXT_PUBLIC_VK_APP_ID`).

**Ключевые архитектурные решения:**
- Хэш-роутинг: глубокая ссылка `vk.com/app<id>#<platformId>`. Решает проблему динамических маршрутов при статическом экспорте (ID площадок неизвестны на сборке).
- «Сейчас» = `serverTime` (UTC) из GAS (время с сети, не с телефона).
- Активность: `time_start <= serverTime < time_end` (UTC). Может быть несколько активных одновременно.
- Фильтр «текущий день» — локальная дата пользователя = serverTime + смещение часового пояса устройства.
- Кэш расписания в localStorage (2ч) + принудительное обновление по «версии кеша».
- Идентификация: `vk_user_id` из launch params (vk-bridge), fallback на localStorage. Защита от подмены не требуется.
- Права на вопрос: редактирование/удаление только если `vk_user_id` вопроса == `vk_user_id` клиента. Удаление — физическое.
- Задержка отправок (вопрос/отзыв) — минимум 10 секунд (клиент + GAS).
- Максимум логов (`Logger.log`/`console.log`) на ранних этапах.

**Границы scope:**
- Только GAS + Google Таблица на бэкенде.
- Только GitHub Pages на фронте.
- Работа внутри VK Mini Apps на мобильных.
- Вопросы/отзывы — простой текст и цифры.
- Несколько вопросов и отзывов от одного пользователя на площадку разрешены.
- Отзывы — без редактирования/удаления.

**Декомпозиция задач:** см. `.agents/tasks.md` (TASK-20260901-01 … TASK-20260901-08).

**Критерии готовности плана:**
- [x] Архитектура согласована с пользователем.
- [x] Задачи декомпозированы, критерии готовности и проверки заданы.
- [x] Учтён `.agents/changelog.md` (дублей нет — проект новый).
